<%= form_for(facilitator) do |f| %>
  <% if facilitator.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(facilitator.errors.count, "error") %> prohibited this facilitator from being saved:</h2>

      <ul>
      <% facilitator.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :cpf %>
    <%= f.text_field :cpf %>
  </div>

  <div class="field">
    <%= f.label :institution_id %>
    <%= f.text_field :institution_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
